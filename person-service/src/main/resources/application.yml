server:  
  port: ${port:9300}

spring:  
  application:
    name: person-service

endpoints:
    shutdown:
      enabled: true
      sensitive: false

envoy:
  discovery:
    url: http://localhost:9200